<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 인터넷 권한: Retrofit API 통신 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 포그라운드 서비스 권한 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- 시스템 오버레이(플로팅 버튼) 권한 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- 미디어 읽기 권한: 스크린샷 URI 접근 (API 33+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- 미디어 읽기 권한: 스크린샷 URI 접근 (API 32 이하) -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- 알림 권한 (API 33+, 포그라운드 서비스 알림 표시) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AIFashion">

        <!-- 메인 액티비티: singleTop으로 설정하여 서비스에서 Intent 재사용 가능 -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.AIFashion">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 크롭 액티비티: 서비스에서 직접 실행 -->
        <activity
            android:name=".ui.crop.CropActivity"
            android:exported="false"
            android:theme="@style/Theme.AIFashion" />

        <!-- uCrop 내부 액티비티 등록 -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <!-- 스크린샷 감지 포그라운드 서비스 -->
        <service
            android:name=".service.CaptureDetectionService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

    </application>

</manifest>
